import styled from 'styled-components'

export const InfoContainer = styled.div`
  display: flex;
  margin-bottom: 0.3rem;

  p:nth-of-type(1) {
    font-weight: 500;
    margin-right: 0.5rem;
    p {
      font-weight: 400;
    }
  }
`
