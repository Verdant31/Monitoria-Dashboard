import styled from "styled-components";

export const InfoContainer = styled.div`
  display: flex;
  margin-bottom: 0.3rem;
  p {
    color: black;
  }
  p:first-child {
    font-weight: 500;
    margin-right: 0.5rem;
  }
`